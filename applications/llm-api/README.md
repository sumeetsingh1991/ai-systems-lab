# LLM API

An application-layer API gateway in front of one or more LLM backends:
routing, auth, rate limiting, and response shaping.

## Layout

- `src/` — service source code
- `tests/` — unit and integration tests
